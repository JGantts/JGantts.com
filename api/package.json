{
  "name": "jgantts-com-api",
  "version": "1.0.0",
  "description": "",
  "main": "dist-dev/index.js",
  "scripts": {
    "dev": "concurrently --kill-others 'npx tsc --watch --outDir \"dev-build\"' 'npx nodemon -q dev-build/index.js'",
    "build": "npm run build-only",
    "build-only": "npx tsc --outDir '../dist/' && cp -r imgs ../dist/imgs/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/multer": "^1.4.7",
    "@types/node": "^18.13.0",
    "concurrently": "^7.6.0",
    "eslint": "^8.34.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jimp": "^0.22.7",
    "lint": "^0.8.19",
    "multer": "^1.4.5-lts.1"
  }
}
