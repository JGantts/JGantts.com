{
  "name": "jgantts-com-monorepo",
  "private": true,
  "workspaces": [
    "jgantts-com",
    "jgantts-server"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names server,app --prefix-colors cyan,magenta \"npm --prefix jgantts-server run dev\" \"npm --prefix jgantts-com run dev\"",
    "dev:app": "npm --prefix jgantts-com run dev",
    "dev:server": "npm --prefix jgantts-server run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
